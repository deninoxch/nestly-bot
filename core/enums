from enum import Enum 


class UserRole(str, Enum):
    USER = 'user'
    ADMIN = 'admin'
    SUPERADMIN = 'superadmin'

class Language(str, Enum):
    RU = 'ru'
    US = 'us'

class AppilacationStatus(str, Enum):
    PENDING = 'pending'
    ACCEPTED = 'accepted'
    REJECTED = 'rejected'

class Country(str, Enum):
    RUSSIA = 'russia'
    TURKEY = 'turkey'